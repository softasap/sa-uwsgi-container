---
# defaults file for sa-uwsgi-container

role_dir: "{{ role_path }}"
option_install_python: false

container_user: uwsgi
container_uid: 1000
container_init: "dumb-init" # to support phusion at later stages
dumb_init_version: "1.2.0"

